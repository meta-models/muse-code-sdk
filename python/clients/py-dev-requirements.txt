# Dev/build tooling for the Python SDK lane (specs/638-muse-sdk-python
# FR-638-002): exact pins only — the CI lane installs from this file, and
# clients/sdk-py/tests/test_manifests.py rejects any non-exact line.
# Build/dev-time only; the packages' RUNTIME posture is INV-638-03.
build==1.6.1
mypy==2.3.1
pydantic==2.13.5
pytest==9.1.1
setuptools==84.0.0
pytest-asyncio==1.4.0
tomli==2.4.1 ; python_version < "3.11"
